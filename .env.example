# 🌍 OpenWeatherMap API Key (OBRIGATÓRIA)
# Obtenha sua chave gratuita em: https://openweathermap.org/api
WEATHER_API_KEY=sua_api_key_openweathermap_aqui

# 🗄️ PostgreSQL Configuration (para Docker)
POSTGRES_USER=mcp_user
POSTGRES_PASSWORD=sua_senha_postgresql_segura_aqui
POSTGRES_DB=weather_mcp

# 🗄️ Database URL completa (para aplicação local)
DATABASE_URL=postgresql://mcp_user:sua_senha_postgresql_segura_aqui@localhost:5432/weather_mcp

# ⚡ Redis Configuration
REDIS_URL=redis://localhost:6379

# 🖥️ Application Configuration
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# 📝 INSTRUÇÕES DE SEGURANÇA:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Substitua TODOS os valores "sua_*_aqui" pelos seus dados reais
# 3. NUNCA commite o arquivo .env no Git
# 4. Se expor acidentalmente, revogue e gere nova API key imediatamente

# 🔐 DICAS DE SEGURANÇA:
# - Use senhas com pelo menos 16 caracteres
# - Inclua letras maiúsculas, minúsculas, números e símbolos
# - Nunca reutilize senhas entre diferentes serviços
# - Considere usar um gerenciador de senhas